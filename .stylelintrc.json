{
  "rules": {
    "color-no-hex": [true, {
      "message": "Use a design token — var(--neutral-…/--primary-…/--status-… etc.), not a raw hex color. See src/tokens/tokens.css."
    }],
    "function-disallowed-list": [
      ["rgb", "rgba", "hsl", "hsla"],
      {
        "message": "Use a design token instead of a raw color function. If you need transparency, add/reuse a token in src/tokens/tokens.css."
      }
    ],
    "property-disallowed-list": [
      ["font-family"],
      {
        "message": "Do not set font-family — Inter is applied globally. Remove this declaration."
      }
    ],
    "declaration-property-unit-disallowed-list": [{
      "font-size": ["px", "em"],
      "line-height": ["px"]
    }, {
      "message": "Use a rem-based design token (var(--font-…)) for font-size, not raw px/em. For line-height, use a unitless ratio (--leading-…). See src/tokens/tokens.css."
    }]
  }
}
